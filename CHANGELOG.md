# تاریخچه‌ی تغییرات (Changelog)

فرمت این فایل بر اساس [Keep a Changelog](https://keepachangelog.com/) است — جدیدترین نسخه بالاست. این فایل همیشه با هر تغییر مهم به‌روزرسانی می‌شود.

---

## v1.2.1 — اصلاح معماری ریشه‌ای Layout

- **App Shell Layout Contract**: یه قرارداد مرکزی جدید (`--app-header-height`, `--app-bottom-nav-inset`, `--app-safe-*` در `index.css`) جایگزین همه‌ی حدس‌های قبلی viewport/safe-area شد.
- نوار پایین شناور حالا با `ResizeObserver` واقعاً اندازه‌گیری می‌شود (نه فرمول حدسی).
- Calculator دیگر هیچ کد مربوط به viewport/safe-area/پلتفرم ندارد — فقط `height: 100%` از یک container که AppShell درست اندازه‌اش کرده.
- همین باگ در کانتینر Toast هم پیدا و رفع شد.
- رفع قطعی مشکل رفتن ردیف آخر کلیدهای ماشین‌حساب زیر نوار پایین (در مرورگر، خصوصاً گوشی‌های با نوار حرکتی).

---

## v1.2.0

### رفع باگ‌های حیاتی
- **فریز دکمه‌ی Back بعد از «Reload Page» تلگرام** — علت ریشه‌ای: نبود اسکریپت رسمی `telegram-web-app.js` در `index.html` باعث می‌شد تشخیص محیط تلگرام بعد از هر بازخوانی صفحه‌ی داخلی شکست بخورد و کل اپ بی‌صدا به حالت وب برگردد.
- **صفحه‌ی سفید/خالی بیرون از تلگرام (وب و لوکال)** — همان اسکریپت رسمی، طبق طراحی خودش، حتی بیرون از تلگرام هم یک شیء stub می‌سازد؛ تشخیص محیط قبلاً فقط وجود این شیء را چک می‌کرد. الان محتوای واقعی (`initData` غیرخالی) چک می‌شود.
- **ریسپانسیو ماشین‌حساب در مرورگر (نه تلگرام)** — نوار پایین ناوبری از `safe-area-inset-bottom` (برای گوشی‌های با نوار حرکتی) استفاده می‌کرد که در فرمول ارتفاع ماشین‌حساب لحاظ نشده بود؛ الان هماهنگ شده + یک فاصله‌ی اطمینان ۱۶ پیکسلی اضافه شد.
- محدودیت ۶۴ کاراکتری `start_param` تلگرام برای Deep Link حالا واقعاً چک می‌شود (فرمول‌های چندمتغیره قبلاً همیشه شکسته بودند).

### قابلیت‌های جدید
- **سیستم مرکزی Share** (`core/sharing/`) برای ماشین‌حساب، فرمول‌ها، و مبدل واحد — با اولویت Telegram native share → Web Share API → Clipboard.
- **Deep Link کامل** برای هر سه بخش (نه فقط ماشین‌حساب) — اشتراک‌گذاری یک فرمول یا تبدیل، لینکی می‌سازد که همان صفحه را با همان مقادیر باز می‌کند.
- **گسترش Haptic Feedback** به کل اپ: حذف آیتم‌ها، پین/علاقه‌مندی، تب‌ها و ناوبری، محاسبه‌ی موفق فرمول‌ها — نه فقط کیبورد ماشین‌حساب.
- نام کاربر تلگرام (فقط نام، بدون عکس/یوزرنیم) در پیام خوش‌آمدگویی صفحه‌ی اصلی.
- بخش «تلگرام» در تنظیمات (فقط داخل Mini App) با کلید روشن/خاموش هپتیک.
- بات تلگرام (`@MathEngineANBot`) برای پاسخ به `/start` با دکمه‌ی باز کردن مستقیم اپ.

---

## v1.1.0 — Telegram Mini App Platform Layer

- زیرساخت کامل Platform Layer (`src/platform/`) با مرز مشخص: هیچ فایلی بیرون از این پوشه مستقیم SDK تلگرام را import نمی‌کند.
- تم زنده (Auto/Light/Dark) هماهنگ با تم تلگرام، بدون نیاز به رفرش.
- Safe Area / Viewport هماهنگ با چیدمان موبایل تلگرام.
- دکمه‌ی Back واقعی تلگرام، هماهنگ با ناوبری داخلی اپ.
- Haptic Feedback پایه روی کیبورد ماشین‌حساب.
- CloudStorage (بک‌آپ تنظیمات و علاقه‌مندی‌ها).
- Fullscreen و Main Button (primitive آماده، بدون UI اضافه).
- رفع چند باگ ریسپانسیو حالت لایت (رنگ پس‌زمینه‌ی سایدبار و Segmented control).

---

## v1.0.0 — انتشار اولیه

- ماشین‌حساب علمی، کتابخانه‌ی فرمول، مبدل واحد، میزکار، تنظیمات.
- پارسر ریاضی امن (بدون `eval`)، ذخیره‌سازی محلی با مکانیزم Migration.
- پشتیبانی کامل فارسی/انگلیسی و RTL.
- PWA قابل‌نصب با پشتیبانی آفلاین.
